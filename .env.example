# Admin Panel — environment variables
# Copy to .env.local and fill in values

# API base URL for data fetching (leave empty to use mock data)
NEXT_PUBLIC_API_URL=

# App metadata
NEXT_PUBLIC_APP_NAME="Admin Panel"
